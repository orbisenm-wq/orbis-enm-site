'use client'
import { useState } from 'react'
export default function ContactForm({type='business'}:{type?:'business'|'creator'}){ const [status,setStatus]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); setStatus('전송 중입니다...'); const data=Object.fromEntries(new FormData(e.currentTarget)); const res=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,type})}); setStatus(res.ok?'문의가 접수되었습니다. 확인 후 연락드리겠습니다.':'전송에 실패했습니다. 이메일 설정을 확인해주세요.')} return <form onSubmit={submit} className="grid gap-4"><input name="name" required placeholder="이름 / 회사명" className="bg-white/[.04] border border-orbis-gold/20 px-5 py-4 outline-none focus:border-orbis-gold"/><input name="email" required type="email" placeholder="이메일" className="bg-white/[.04] border border-orbis-gold/20 px-5 py-4 outline-none focus:border-orbis-gold"/><input name="phone" placeholder="연락처" className="bg-white/[.04] border border-orbis-gold/20 px-5 py-4 outline-none focus:border-orbis-gold"/>{type==='creator'&&<input name="channel" placeholder="채널 URL / SNS 계정" className="bg-white/[.04] border border-orbis-gold/20 px-5 py-4 outline-none focus:border-orbis-gold"/>}<textarea name="message" required placeholder="문의 내용" rows={6} className="bg-white/[.04] border border-orbis-gold/20 px-5 py-4 outline-none focus:border-orbis-gold"/><button className="mt-2 bg-orbis-gold px-6 py-4 font-black tracking-[.18em] text-black hover:bg-[#e3c474]">SEND</button>{status&&<p className="text-sm text-orbis-gold">{status}</p>}</form> }
